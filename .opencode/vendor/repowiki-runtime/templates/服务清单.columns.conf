# 服务清单列配置。格式：列名 = 来源键
# 可用来源键：
# index, iface_short, service_iface, iface_qn, impl_qn, module, module_cat,
# config, version, profile, entry_type, base_path

@title = {app} 服务清单
序号 = index
服务名称 = iface_short
服务分类 = module_cat
服务实现类全限定名 = impl_qn
服务接口全限定名 = iface_qn
配置方式 = config
版本 = version
